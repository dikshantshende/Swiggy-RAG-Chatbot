services:
  - type: web
    name: swiggy-rag-api
    env: python
    runtime: python3.10
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GEMINI_API_KEY
        sync: false
